export const ROUTES = {
  home: '/',
  login: '/login',
  register: '/register',
  app: '/app',
  tickets: '/app/tickets',
  createTicket: '/app/tickets/create',
  notifications: '/app/notifications',
  profile: '/app/profile',
} as const;
